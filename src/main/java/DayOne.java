import java.util.HashMap;

public class DayOne {

    private HashMap<Integer, Integer> mapofElves;

    private HashMap<Integer, Integer> streamAndCreateMap() {
        //stream in line by line and add values to hashmap
        // return the hashmap
        mapofElves = new HashMap<>();
        return mapofElves;
    }

    private int getHighestCalories(HashMap<Integer, Integer> listOfElves) {
        return 0;
    }

}
